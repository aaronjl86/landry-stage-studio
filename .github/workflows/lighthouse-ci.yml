name: Lighthouse CI
on: [push]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: treosh/lighthouse-ci-action@v10
        with:
          urls: 'https://thelandrymethod.com'
          temporaryPublicStorage: true
